# SPEC.md — 月圓之夜(中秋互動故事)

## 要做什麼

一個以中秋節為主題的分支互動故事網頁。使用者透過點擊選項,在「和家人烤肉」與「獨自賞月」兩條主線間做選擇,每個選擇會導向不同的後續劇情,最終抵達其中一種結局。整個體驗是單頁、單次載入,不需要重新整理頁面。

## 主要功能

1. **故事播放**:載入頁面即從開頭節點開始顯示劇情文字。
2. **分支選擇**:每個劇情節點(結局除外)顯示 2 個選項按鈕,點擊後切換到對應的下一個節點。
3. **結局呈現**:抵達結局節點時,顯示結局名稱與描述文字,並提供「重新開始」按鈕。
4. **重新開始**:點擊重新開始後,狀態重置回開頭節點,可重新遊玩以體驗不同結局。
5. **進度指示**:非結局畫面上方顯示目前所在步數的進度條。

## 系統行為

- 整體採**有限狀態機**設計:任一時刻頁面只呈現一個「節點」的內容,節點資料與畫面渲染邏輯分離。
- 所有故事內容(節點文字、選項、結局)以 JavaScript 物件形式寫在檔案內,不透過任何外部 API 或資料庫讀取。
- 使用者的選擇**不會被儲存**(重新整理頁面即從頭開始),本版本不使用 localStorage 記錄進度。
- 純前端渲染,沒有後端伺服器、沒有網路請求。
- 支援鍵盤 focus 樣式與 `prefers-reduced-motion`,對可及性(accessibility)有基本考量。

## Acceptance Criteria

- [ ] 打開網頁後,3 秒內可看到故事開頭文字與 2 個選項按鈕。
- [ ] 點擊任一選項後,畫面正確切換到對應的下一段故事,不會停留在原節點或跳錯節點。
- [ ] 每條分支路徑(烤肉線、賞月線)各自都能走到至少一種結局,不會出現「無選項可點」但又不是結局的死路。
- [ ] 抵達結局畫面時,顯示對應的結局標題與描述文字。
- [ ] 點擊「重新開始」後,畫面回到故事開頭,且可以重新選擇並抵達不同結局。
- [ ] 在手機寬度(375px 左右)瀏覽時,文字與按鈕不會超出畫面或被截斷。
- [ ] 網頁可在 GitHub Pages 等靜態託管服務上直接開啟並正常運作,不需要任何後端服務。

## Non-goals(這次不做的事)

- 不做使用者帳號、登入系統。
- 不做遊玩進度的雲端或本機儲存(每次重新整理都是全新一輪)。
- 不做後端 API、資料庫、排行榜或留言板。
- 不支援多語言切換(僅提供繁體中文版本)。
- 不做音效或背景音樂。
- 不追求跨所有舊版瀏覽器相容,以現代瀏覽器(Chrome / Safari / Edge / Firefox 近兩年版本)為目標。
